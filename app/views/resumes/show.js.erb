if($(document).width() < 992){
	location = '<%= resume_path(@resume) %>';
}
$('#job_info').html('<%= escape_javascript(render(partial: "pages/partials/resume", locals: {resume: @resume})) %>')

var offset = $('.info-cards .info-cards__card:first').offset()
if(typeof offset != 'undefined')
{
	if($(document).scrollTop() > offset.top){
	  $("#job_info").addClass("fix-block");
	} else {
	  $("#job_info").removeClass("fix-block");
	}
}
