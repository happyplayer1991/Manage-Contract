<div class="row">
  <div class="col-md-3">
    <%= image_tag placeholder_img(@company.logo, 'logo'), width: '100%' %><br><br>
  </div>

  <div class="col-md-9">
    <h2><%= @company.title %></h2>
    <h4>Take a minute to review this company:</h4>
  </div>
</div>

<%= render 'form', review: @review %>
<%= link_to "Back to #{@company.title}", company_path(@company), class: 'btn btn-primary' %>
<br>
<br>
